<div>
    <div>注意：没有资产编号的资产将不会出现在这里。</div>
    <div>复制以下全部内容到 <code>input.txt</code> 文件中：</div>
    <textarea class="form-control" rows="15" readonly>@foreach($data as $item)
            {!! $item !!}
        @endforeach</textarea>
</div>
